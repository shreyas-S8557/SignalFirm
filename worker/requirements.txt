fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
redis>=5.0
rq>=1.16
python-dotenv>=1.0
apscheduler>=3.10  # recommendations_scheduler_main.py's daily digest trigger, outbound_scheduler_main.py's sequence sweep
beautifulsoup4>=4.15.0  # enrichment/website_crawler.py's HTML text extraction
pyyaml>=6.0  # icp/rubric.py's data/icp_rubric.yaml
prometheus-client>=0.20  # observability.py's /metrics endpoint
# sentry-sdk>=2.0  # optional -- uncomment and set SENTRY_DSN to enable error reporting (see observability.py)

# dev/test only
pytest>=8.0
pytest-mock>=3.14
pytest-cov>=5.0
ruff>=0.6  # CI lint step, see .github/workflows/ci.yml
locust>=2.29  # loadtest/locustfile.py
